
services:
  # go-api:
  #   build: 
  #     context: ./backend/go-api
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8080:8080"
  #   volumes:
  #     - ./backend/go-api:/app
  #   environment:
  #     - PYTHON_SERVICE_URL=http://python-service:5000
  #     - ENV=development
  #   depends_on:
  #     - python-service
  #     - postgres

  python-service:
    build:
      context: ./backend/python-service
      dockerfile: Dockerfile
    ports:
      - "5000:5000"  # Exposed for development
    volumes:
      - ./backend/python-service:/app
      - pip_cache:/root/.cache/pip  # Cache pip packages
    environment:
      - DATABASE_URL=${SUPABASE_DATABASE_URL}
      - ENV=development
    command: uvicorn app.main_test:app --host 0.0.0.0 --port 5000

  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./frontend:/app
  #     - /app/node_modules
  #   environment:
  #     - VITE_API_URL=http://localhost:8080
  #   depends_on:
  #     - python-service

volumes:
  pip_cache: